name: Build+Test

on:
  push:
    branches: [ "master", "*" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v3
    - name: configure
      run: echo "no configure"
    - name: make
      run: make test
  build-linux:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: configure
      run: echo "no configure"
    - name: make
      run: make test
